{
  "name": "chess-utility",
  "version": "1.24.1",
  "private": true,
  "description": "Chrome extension: type chess moves instead of dragging them, plus keyboard navigation on any site",
  "scripts": {
    "test": "node test/matcher.test.js && node test/engine.test.js && node test/hints.test.js",
    "package": "bash tools/package.sh"
  }
}
